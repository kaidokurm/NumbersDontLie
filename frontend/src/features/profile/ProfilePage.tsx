import { ProfileContainer } from "./ProfileContainer";

// ProfilePage: Route page component that renders the profile container
export default function ProfilePage() {
    return <ProfileContainer />;
}
